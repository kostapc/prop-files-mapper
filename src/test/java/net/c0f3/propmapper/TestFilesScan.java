package net.c0f3.propmapper;

/**
 * Created by kostapc on 11.11.16.
 *
 */
public class TestFilesScan {

    public void testFilesScan() {


        /*Map<String, Partner> partners = scanner.scan();

        Collection<Partner> partnersStored = partnerData.getPartners();
        partnersStored.forEach( (partner) -> {
            Partner partnerFromFile = partners.get(partner.getPartnerId());
            if(partnerFromFile==null) {
                return;
            }
            if(scanner.isDifferent(
                    partner, partnerFromFile
            )) {
                partnerData.upsertObject(partnerFromFile, Partner.KEY_FIELD, partner.getPartnerId());
                LOG.info(String.format(
                        "updating partner: %s",
                        partner.getPartnerId()
                ));
            }
        });*/
    }

}
